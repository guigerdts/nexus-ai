# Auditoría NEXUS AI — Sesión guardada (2026-06-11)

## Estado
Auditoría completa de bugs, issues de código, y categorización por riesgo.

## Hallazgos pendientes de aplicar (prioridad segura)

### ✅ Listos para aplicar (riesgo bajo)
1. **M5** — `config/env.sh` L51: eliminar `2>/dev/null` sin efecto en `elif`
2. **B3** — `lib/nexus-log.sh` `show_banner`: agregar `[ -t 1 ]` para no emitir ANSI en pipes
3. **B5** — `core/nexus.sh` `remove_agent()`: eliminar asignación de variable `_pkg` no usada
4. **B11** — `core/nexus.sh` `list_agents()`: limpiar `local` + re-asignación confusa
5. **B7** — `tui/system_monitor.py`: mejorar mensaje cuando hay timeout
6. **B6** — `tui/guide.py`: refrescar tabla post-instalación
7. **M2** — `tui/agents_panel.py`: alinear chequeo "instalado" con CLI (leer installed.txt)

### ⚠️ No tocar (riesgo alto)
- A1 — `install_via_binary` con `file://` (fallback funciona)
- A2 — Cache guard-3 (riesgo de performance)
- A3 — Refactor `list_agents()` 160 líneas duplicadas (requiere tests)
- M3 — VA39 patching embebido (crítico Android)
- M4 — Subshell quoting en `remove_agent` (desinstalación)
- B1 — 21 módulos sin install.sh (feature, no fix)
- N1-N5 — No-bugs verificados

## Comandos útiles para retomar
```bash
cd /root/nexus-ai
# Verificar sintaxis bash
bash -n core/nexus.sh config/env.sh lib/nexus-log.sh
# Verificar sintaxis python
python3 -m py_compile tui/dashboard.py tui/agents_panel.py tui/guide.py
```
